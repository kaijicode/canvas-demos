import { add } from './index';

describe('checkAABB', () => {
    it('should do something', () => {
        expect(add(1, 2)).toEqual(3);
    });
});


describe('distance', () => {
    it('should do something', () => {
        expect(add(1, 2)).toEqual(3);
    });
});

